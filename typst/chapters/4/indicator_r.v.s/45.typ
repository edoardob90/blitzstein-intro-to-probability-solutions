Notice that $I(A_{1} sect A_{2} dot.c sect A_{n}) gt.eq sum_(i=1)^(n)I(A_{i})
- n + 1$ because the left-hand side is either $0$, or $1$, so the question
reduces to whether the left-hand side is ever $0$, while the right-hand side is
$1$. Notice that this is not possible, becuase if the left-hand side is $0$,
then $A_{j} = 0$ for some $j$. Thus, $sum_(i=1)^(n)I(A_{i}) < n arrow.r.double \text
{R.H.S.} < 1$.

Then,

$I(\bigcap_{i=1}^{n}A_{i}) gt.eq sum_(i=1)^(n)I(A_{i})
- n + 1 arrow.r.double \text{E}(I(\bigcap_{i=1}^{n}A_{i})) gt.eq \text{E}
(sum_(i=1)^(n)I(A_{i}) - n + 1) arrow.r.double P(\bigcap_{i=1}^{n}A_{i})
gt.eq sum_(i=1)^(n)P(A_{i}) - n + 1$