(a)
\begin{flalign}
    \Phi(W)(t)  = P(\Phi(z)^2 lt.eq t) 

     = P(\Phi(z) lt.eq sqrt(t)) 

     = P(z lt.eq \Phi^{-1}(sqrt(t))) 

     = \Phi(\Phi^{-1}(sqrt(t))) 

     = sqrt(t)
\end{flalign}

(b)
$bb(E)[W] = integral_(0)^(1) w^3 f_W(w) dw$
$bb(E)[W] = integral_(0)^(1) \Phi(z)^6 \phi(z) dz$

(c)
$P(X + 2Y < 2Z + 3) = P(X + 2Y - 2Z < 3)$
here, $X + 2Y - 2Z \sim N(0, sqrt(1^2 + 2^2 + 2^2)) $ 
So, $P(X + 2Y < 2Z + 3) = \Phi(1)$